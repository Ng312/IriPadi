# Keep annotations and signatures for Firebase/JSON and general safety.
-keepattributes Signature
-keepattributes *Annotation*

# Uncomment and tailor additional keep rules if you introduce reflection-heavy libs.
#-keep class com.yourpackage.** { *; }
